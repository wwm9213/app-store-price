# 本地验收记录

本文保留发布前各轮本地验收的历史状态；文中的“未提交、推送或部署”指对应验收时点。最新远程构建和交付状态以 [GitHub Actions](https://github.com/wwm9213/app-store-price/actions) 为准。

日期：2026-09-16（Asia/Shanghai）。基线 `87addff6788eba1a615ec1a92262a46a9715ddb6`，本地分支 `codex/global-storefront-comparison`。本次未提交、推送或部署。

## 本轮交互与默认范围调整

- Maven `verify`：**53 项 Java 测试全部通过**；Node：**9 项全部通过**。最新静态资源修改后重新运行 Node 测试，并重新打包 JAR。
- 默认范围为 14 个主流地区；新增地区选择、集合归一化、共享查询隔离、真实 HTTP 默认范围与无效范围回归。原 13 区旧接口保持不变。
- 默认首页输入为空，无自动 App 查询。名称搜索 X 返回候选后保持待选状态，点击 App ID `333903271` 后才查询价格；单个候选也需选择（自动化覆盖）。
- 新版 JAR 在 `18080` 实测 X 主流查询：**14 / 14 成功、0 不可售、0 失败**；12 个应用与内购入口，23 个创作者订阅入口，分类展示。
- 用此前 X 全量快照复核显示层归并：原 354 个项目折叠为 196 个名称入口，其中 159 条 `Promote Post` 变为 1 个入口；不同创作者未合并。同名多档金额及可靠商品身份保留，原始快照不变。真实裁剪 fixture 回归奥地利同名不同价，不以跨档最低值做排名。
- 内置浏览器实测：自选 CN / HK / AT 只查询 3 区；修改范围保留当前购买项目；刷新首页输入为空、地区偏好保留；重复查询显示本地缓存；清除缓存后地区选择仍保留；分享链接保留实际地区集合，不覆盖偏好。验收后恢复原有地区选择，范围设回主流。
- 桌面默认视口 1055×956、手机 390×844：无横向溢出，手机为卡片，项目列表分别限制为 392 / 174 px；验证项目搜索、分类、明暗主题、手机地区弹窗。控制台无 error / warn。
- 新截图：[桌面](screenshots/desktop-v2.jpg)、[手机](screenshots/mobile-v2.jpg)。预览停留在无默认输入的首页。
- 本轮未重新构建 Docker；下方 Docker、全量 175 区与下载记录是首轮实施的历史验收，不能当作本轮 Docker 复验。未提交、推送或部署。

## 首轮实施记录

以下保留首轮全量抓取与容器验收证据；当时界面与默认全量行为已由本轮修改替代。

## 自动化

- `JAVA_HOME=<JDK 21> ./mvnw -q verify`：50 项 Java 测试，0 失败、0 错误、0 跳过，打包成功。
- `node --test src/test/js/*.test.cjs`：3 项通过。
- `git diff --check`：通过。
- 覆盖真实页面两种 IAP 结构、币种和地区分离、缺失价格、付费/明确免费、未公开 IAP、单项失败、跨区跳转、重复名称、同区英文别名及顺序反转。
- 覆盖并发上限、队列满、请求合并、缓存过期与不可售缓存、429/5xx 重试、404 不重试、汇率旧值回退、SSE 重连/心跳/完成、失败地区单独重试及旧五接口兼容。
- 付费与部分错误结构使用合成场景，不冒充线上应用验收。

## 真实 Apple 查询

- 本地 JAR（端口 18080），App：`1546947240`，完整 175 storefront 配置。最后一轮 10:24:04 开始，10:26:37 最后地区返回，约 154 秒。
- 最终：**175 成功、0 不可售、0 失败**；本体、永久会员、季度会员均覆盖 175 区。两项 IAP 均为 `INFERRED`，未获得可靠商品 ID。
- 早期一轮出现亚美尼亚、比利时 TLS 握手失败；后续一轮利比亚出现相同失败。API 与页面“重试失败地区”均恢复成功。上述外部失败未被当作不可售。
- 发现并修复印尼 `Rp 99ribu` / `Rp 29ribu` 的缩写单位，真实接口确认金额为 99,000 / 29,000 IDR；增加真实 fixture 和回归用例。
- 所有 43 种配置币种均有汇率。固定快照日期 `2026-09-15`，来源 Currency API；以下原币价格不依赖换算。

| storefront | 币种 | 永久会员原始文本 | 季度会员原始文本 |
|---|---|---|---|
| US | USD | $4.99 | $1.99 |
| CN | CNY | ¥36.80 | ¥16.80 |
| HK | HKD | HK$ 38.00 | HK$ 18.00 |
| TW | TWD | $150.00 | $60.00 |
| JP | JPY | ¥800 | ¥300 |
| KR | KRW | ￦7,700 | ￦3,300 |
| TR | TRY | ₺249,99 | ₺99,99 |
| IN | INR | ₹ 499 | ₹ 199 |
| SG | SGD | S$ 6.98 | S$ 2.98 |
| GB | GBP | £4.99 | £1.99 |
| CA | CAD | $6.99 | $2.99 |
| AU | AUD | $7.99 | $2.99 |

澳洲原始页面季度会员排在永久会员之前，归并结果仍正确。

原始验收导出：[JSON](verification/1546947240.json)、[CSV](verification/1546947240.csv)。只保存本次验收快照，应用未引入历史价格数据库。

## 页面与接口

- Codex 内置浏览器：桌面 1280×1000、手机 390×844，均无横向溢出；手机正确切换为卡片。
- 实际验证：查询中逐步展示、名称搜索候选、候选选择、URL / ID 直查、商品切换、中英文地区搜索、分区筛选、基准比较、分地区详情、我的地区保存、分享链接、失败重试。
- 页面控制台无 error / warn。截图见 [桌面](screenshots/desktop.jpg) 与 [手机](screenshots/mobile.jpg)。
- 内置浏览器取消文件下载，改用 Chrome 原生界面补验：CSV 和 JSON 均实际保存成功。临时 Chrome 标签页已关闭。
- CSV 700 行数据：525 条价格 + 175 条地区状态。用实际浏览器下载的 JSON 重新生成 CSV，与浏览器下载的 CSV 逐字节一致。
- 真实 `/app/getAppInfo` 返回原 13 区；完成状态 SSE 直接返回 `event:complete`，无普通 JSON 封装。未知 v2 路径为 HTTP 404 JSON，无效 App ID 为 HTTP 400 JSON。

## Docker

- 已安装并启动 Colima / Docker 本地引擎，修复新建 VM 的 DNS 后完成验收。
- `docker compose --progress plain build`：从源码构建最终版本，Docker 构建上下文排除 `target/`，未复制本地预编译 JAR。构建阶段再次执行 **50 项 Java 测试，全部通过**。
- `APP_PORT=127.0.0.1:18081 docker compose up -d --no-build --wait`：成功，容器状态 **healthy**，运行用户 `app`。
- 容器 `/api/v2/storefronts` 返回成功封装与 175 区；首页内容与最终源文件逐字节一致，价格 API 已返回 42 个成功地区，含 US / CN / HK / TW / KR，容器中印尼价格确认为 99,000 / 29,000 IDR。此回读时容器查询尚在继续；完整 175 区验收来自上面的本地 JAR 查询。
- 本次 Docker 验收为本机 ARM64 镜像；未宣称其他 CPU 架构、远程发布或部署已验证。

## 验收边界

175/175 是上述样例和本次网络环境的观测结果，不代表所有应用、所有时刻均可售。公开 IAP 可能不完整，推断匹配保留标记，外部限流和解析失败有单独状态。CI 配置已添加；未推送，未宣称远程 CI 或部署通过。
